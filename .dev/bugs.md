# Bugs

Known defects not yet fixed. One heading per bug: symptom, where, repro, status.

- `.agents/skills/start-gones-server/SKILL.md` still describes a Vite server on `:5173`; `npm run dev` serves Angular on `http://127.0.0.1:4200`.
