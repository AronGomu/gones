/node_modules
/out
/dist
/.angular
**/bin/
**/obj/
/cypress/screenshots
/cypress/videos
/cypress/downloads
.env
.agentsystem/
# Generated quality-gate reports (supply-chain audit, etc). CI retains them as artifacts.
/reports/
# Locally restored dotnet tools (dotnet-ef) used by the migration scripts.
/backend/.tmp-tools/

# T29 hundredfold stress dataset: generated by `npm run dev:stress:generate`, several megabytes, and
# reproducible from its seed. Only the manifest that declares the environment is committed.
/fixtures/dev-environments/stress/*.json
!/fixtures/dev-environments/stress/environment.json

/.pi/artifacts/
# make-aron-v2 quality-gate scratch: run ledgers, coverage and mutation workspaces.
# The config (gates.json, cmd/, layers.json, equivalent-mutants.json) is committed.
/coverage/
/.make-aron/runs/
/.make-aron/.cov/
/.make-aron/.stryker-tmp/
/.make-aron/.stryker-net/
/.make-aron/.complexity.csv
# Graphify knowledge graph: rebuilt locally with `graphify extract . --code-only`.
/graphify-out/
# Test CA exported by the C41 release rehearsal; recreated on every run.
/.release-test-export/
